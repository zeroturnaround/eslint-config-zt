# ZeroTurnaround's ESLint config

## Installation
```shell
$ npm install --save-dev github:zeroturnaround/eslint-config-zt
```

## Usage
```yaml
// .eslintrc.yml
---
extends:
  - "zt"

rules:
    # Some overrides
    no-fallthrough: 0
```